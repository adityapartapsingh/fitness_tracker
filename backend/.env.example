

# Keep this file secret and do NOT commit it to git. Backend .gitignore already ignores .env

AI_PROVIDER=gemini
GEMINI_API_KEY=AIzaSyAD9JfHP0iTzrB-EqKd-teAqvZyL9WaAXI
GEMINI_API_URL=https://generativelanguage.googleapis.com/v1beta/models/gemini-2.0-flash:generateContent

# Optional: set a provider URL if required by your Gemini-compatible API wrapper
# GEMINI_API_URL=https://api.example.com/v1/chat
# GEMINI_MODEL=gemini-2.0-flash

# Keep this file secret and do NOT commit it to git. Backend .gitignore already ignores .env# MongoDB Configuration
MONGODB_URI=mongodb+srv://adityapartapsingh92_db_user:rJpnM19withuV4wp@cluster0.izjhalr.mongodb.net/?appName=Cluster0

# JWT Configuration
JWT_SECRET=your_jwt_secret_key_here

# Server Configuration
PORT=5000
NODE_ENV=development

# Email Configuration Options
# Choose ONE of the following methods:

# Option 1: Gmail with App Password (Recommended)
# 1. Go to https://myaccount.google.com/
# 2. Go to Security → 2-Step Verification → App Passwords
# 3. Generate an app password for "Mail" and "Windows Computer"
# 4. Copy and paste the 16-character password below (remove spaces)
GMAIL_USER=projecttesting38@gmail.com
GMAIL_APP_PASSWORD=hvscrtqfrfzbvrbk

# Option 2: Custom SMTP Server
# SMTP_HOST=smtp.example.com
# SMTP_PORT=587
# SMTP_USER=your-email@example.com
# SMTP_PASS=your-password

# General Email Configuration
# EMAIL_FROM=noreply@fitness-tracker.app
# in backend folder
